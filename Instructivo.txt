------

LAUNCHERS:

Linux:
 	- abrir terminal en la carpeta raiz y ejecutar "./launcher.sh"
 
Windows:
	- abrir powershell en la carpeta raiz y ejecutar ".\launcher-windows.ps1"	

------

APIS:

Api usuarios -> http://localhost:8080/swagger-ui.html
Api base -> http://localhost:8081/swagger-ui.html

------

GIT:

git pull
Descarga los cambios del repositorio remoto y los fusiona con tu rama local actual.

git add .
Agrega todos los archivos modificados o nuevos al área de staging, listos para hacer commit.

git commit -m "mensaje"
Guarda en el repositorio local los cambios preparados, con un mensaje descriptivo.

git push
Envía los commits locales al repositorio remoto (por ejemplo, GitHub).

git diff
Muestra las diferencias entre los archivos modificados y el último commit.

git status
Muestra el estado actual del repositorio: qué archivos cambiaron y cuáles están en staging.

git reset
Deshace cambios. Puede usarse para quitar archivos del staging o volver a un commit anterior (con cuidado).

------

Para el Benja:

1) primera linea: Abrir infraestructura y pegar este código:
Get-Content backup.sql | docker exec -i FlexInventory psql -U flexinventory flexinventory

2) Dentro de api_base y api_usuarios pegar este código:
mvn --% -Dspring-boot.run.jvmArguments="-Duser.timezone=America/Argentina/Buenos_Aires" clean spring-boot:run

3) en Frontent pegar esta linea: 
npm start

